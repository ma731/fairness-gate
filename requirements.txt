# Pinned loosely: this has to keep working on CI months from now, and the audit numbers
# are regenerated rather than trusted from cache, so a minor version bump is visible.
folktables>=0.0.12
fairlearn>=0.10
numpy>=1.26
pandas>=2.0
scikit-learn>=1.6
scipy>=1.11
lightgbm>=4.0
joblib>=1.3
pyyaml>=6.0
